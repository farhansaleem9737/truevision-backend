# TrueVision AI service — pinned to versions known to work with Python 3.10+.
# Total install size: ~2 GB (torch is the bulk). Models add another ~200 MB
# on first use (sentence-transformers + NudeNet weights auto-download).

fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0

# DistilBERT inference for /predict
torch>=2.1.0
transformers>=4.36.0

# FAQ chatbot embedding model
sentence-transformers>=2.5.0
numpy>=1.26.0

# NSFW moderation
nudenet>=3.4.0
pillow>=10.1.0

# HTTP client used to fetch Cloudinary frames
httpx>=0.26.0
